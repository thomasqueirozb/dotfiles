# Flameshot
Print
    flameshot gui

# dmenu
{super + d}
    dmenu_run -fn 'UbuntuMono Nerd Font Regular-17'

{super + x}
    DISPLAY=:0 xkill

{super + shift + x}
    pkill xkill

# Terminator
super + Return
    terminator

super + ctrl + space
    kill -s USR1 $(pidof deadd-notification-center)

F9
    /home/thomas/wps_draw.sh
